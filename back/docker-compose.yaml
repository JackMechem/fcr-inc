services:
  app:
    build:
      context: .
      args:
        DB_URL: ${DB_URL}
        DB_USER: ${DB_USER}
        DB_PASSWORD: ${DB_PASSWORD}
    image: fcr-backend
    container_name: fcr-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
